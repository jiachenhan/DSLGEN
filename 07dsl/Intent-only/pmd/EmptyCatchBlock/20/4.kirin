tryCatchBlock tc where
    tc.catchClauses contain catchClause cc where
        and(
            cc.body.size() == 0,
            cc.exceptionType.name == "SQLException" or cc.exceptionType.name == "JSchException"
        );