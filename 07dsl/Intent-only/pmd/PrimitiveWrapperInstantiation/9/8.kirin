objectCreationExpression oce where
    and(
        oce.name match "Integer|Long|Short|Byte|Float|Double|Character|Boolean",
        not(
            oce in functionCall where
                and(
                    name match "valueOf",
                    base.name match "Integer|Long|Short|Byte|Float|Double|Character|Boolean"
                )
        )
    );