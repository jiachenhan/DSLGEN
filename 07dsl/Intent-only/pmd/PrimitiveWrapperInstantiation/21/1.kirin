objectCreationExpression oce where
    and(
        oce.name == "Long",
        not(
            oce.arguments[0] is functionCall where
                and(
                    functionCall.name == "valueOf"
                )
        )
    );