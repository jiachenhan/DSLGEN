methodCallExpression mc where
    and(
        mc.name == "equals",
        mc.arguments[0] arg where
            arg is VariableAccess,
        not(
            mc.base is StringLiteral
        )
    );