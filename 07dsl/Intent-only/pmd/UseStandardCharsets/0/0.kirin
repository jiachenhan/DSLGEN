functionCall fc where
    and(
        fc.name == "forName",
        fc.base.name == "Charset",
        fc.arguments[0] arg where
            arg is stringConstant where
                arg.value == "UTF-8"
    );