functionCall fc where
    and(
        fc.name == "toString",
        fc.arguments[1] arg where
            and(
                arg is functionCall,
                arg.name == "forName",
                arg.base.name == "Charset",
                arg.arguments[0] == "UTF-8"
            )
    );