variableDeclaration vd where
    and(
        vd.type.name in ["Connection", "PreparedStatement", "ResultSet"],
        vd.initializer is objectCreationExpression,
        not(
            vd in tryWithResourcesStatement
        )
    );