functionCall fc where
    and(
        fc.name == "printStackTrace",
        fc.base is VariableAccess where
            fc.base.name == "e",
        not(
            fc in functionCall where
                fc.base.name match "log.*"
        )
    );