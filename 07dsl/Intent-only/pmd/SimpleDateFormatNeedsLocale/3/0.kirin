objectCreationExpression sdf where
    and(
        sdf.name == "SimpleDateFormat",
        not(
            sdf.arguments contain arg where
                arg.type.name == "java.util.Locale"
        )
    );