functionDeclaration fd where
    and(
        fd.name == "isDone",
        fd.enclosingClass.name == "TrackedRunnable",
        not(fd.annotations contain annotation where annotation.name == "Override")
    );